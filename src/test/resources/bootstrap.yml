spring:
  application:
    name: playlist-music-svc
  cloud:
    config:
      enable: false

eureka:
  client:
    enable: false

spotify:
  url:
    api: https://api.spotify.com/v1
    account: https://accounts.spotify.com/api
  limit: 10
  client-id: 123456
  client-secret: 123456
  credentials: client_credentials